import { colors } from "@/styles/theme";
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  distance: {
    fontSize: 80,
    fontWeight: "bold",
    color: colors.white,
  },
});
